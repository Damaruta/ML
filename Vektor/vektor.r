# 1
v <- c(2, 3, -1, 2)
w <- c(-2, 3, 3, -1)

norm_v <- norm(v)
norm_v

distance(v, w) = ||v - w||
               = sqrt((v1 - w1)^2 + (v2 - w2)^2 + ... + (vn - wn)^2)

distance_vw <- norm(v - w)
distance_vw
